# E2E 스모크 전용 빌드 모드 (vite build --mode e2e) — 중앙 서버·Supabase 없이 로컬(localStorage) 모드로 동작.
# 핵심 흐름(가입→온보딩→고객→예약→결제)이 외부 의존 없이도 깨지지 않는지 CI에서 확인한다.
VITE_AUTH_API_URL=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_GOOGLE_CLIENT_ID=
VITE_PORTONE_IMP_CODE=
VITE_PORTONE_PG=
